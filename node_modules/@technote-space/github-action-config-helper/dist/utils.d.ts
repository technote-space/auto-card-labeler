import { Context } from '@actions/github/lib/context';
import { Octokit } from '@technote-space/github-action-helper/dist/types';
export declare const getConfig: (fileName: string, octokit: Octokit, context: Context, options?: {
    configPath?: string;
    ref?: string;
}) => Promise<false | {
    [key: string]: any;
}>;
