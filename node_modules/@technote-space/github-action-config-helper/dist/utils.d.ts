import { GitHub } from '@actions/github/lib/github';
import { Context } from '@actions/github/lib/context';
export declare const getConfig: (fileName: string, octokit: GitHub, context: Context, configPath?: string) => Promise<false | object>;
